ninja
trimesh
cmake
splines
numpy==1.26.4
tqdm==4.67.1
imageio==2.37.0
nerfacc==0.5.3
nerfview==0.1.3
confluent-kafka==2.12.2
jupyterlab>=4
pandas==2.2.3
einops==0.8.1
lpips==0.1.4
jaxtyping==0.3.2
opencv-python
pytorch-msssim
tensorboard
viser
rich
matplotlib

# Step 1: 
# conda create -n nerf-env python=3.11 -y
# conda activate nerf-env
# Step 2: Install PyTorch with CUDA 11.8 support
# pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 torchaudio==2.1.2+cu118 \
#  --index-url https://download.pytorch.org/whl/cu118
# Step 3: Install tiny-cuda-nn for PyTorch
# pip install --no-build-isolation "git+https://github.com/NVlabs/tiny-cuda-nn/#subdirectory=bindings/torch"
